{
    "parameters": {
        "param": "%general.env%"
    },

    "routes": {
        "/test/%param%/:test" : {
            "controller": "wx.demo.controllers.DemoController",
            "action": "test"
        }
    }
}